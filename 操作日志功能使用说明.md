# 操作日志功能使用说明

## 功能概述

基于AOP实现的操作日志功能，可以自动记录系统中关键操作的详细信息，包括操作人、操作时间、请求参数、执行耗时等。

## 数据库表设计

### sys_log 表结构

```sql
CREATE TABLE sys_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    username VARCHAR(50) NOT NULL COMMENT '操作人',
    operation VARCHAR(200) NOT NULL COMMENT '操作描述',
    method VARCHAR(200) NOT NULL COMMENT '请求方法全路径',
    params TEXT COMMENT '请求参数JSON',
    ip VARCHAR(50) COMMENT '操作IP',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    execution_time BIGINT NOT NULL DEFAULT 0 COMMENT '执行耗时(ms)',
    INDEX idx_username (username),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统操作日志表';
```

**执行建表SQL：**
```bash
mysql -u root -p your_database < wms/src/main/resources/sql/sys_log.sql
```

## 核心组件

### 1. @Log 自定义注解

位置：`com.wms.common.Log`

使用方式：
```java
@Log("操作描述")
@PostMapping("/save")
public Result save(@RequestBody Goods goods) {
    // 业务逻辑
}
```

### 2. LogAspect 切面类

位置：`com.wms.aspect.LogAspect`

功能：
- 拦截带有 `@Log` 注解的方法
- 记录方法执行前后的时间差（执行耗时）
- 获取请求参数并转换为JSON格式
- 获取客户端IP地址
- 从Session中获取当前操作用户名
- **异步保存日志**到数据库（不阻塞主业务流程）

### 3. SysLogController 查询接口

位置：`com.wms.controller.SysLogController`

提供分页查询接口：
- **接口地址：** `GET /log/listPage`
- **请求参数：**
  - `pageNum`: 页码（默认1）
  - `pageSize`: 每页大小（默认10）
  - `username`: 操作人（可选，支持模糊查询）
  - `startTime`: 开始时间（可选，格式：yyyy-MM-dd HH:mm:ss）
  - `endTime`: 结束时间（可选，格式：yyyy-MM-dd HH:mm:ss）

**示例请求：**
```bash
# 查询所有日志
GET /log/listPage?pageNum=1&pageSize=10

# 按操作人查询
GET /log/listPage?pageNum=1&pageSize=10&username=admin

# 按时间范围查询
GET /log/listPage?pageNum=1&pageSize=10&startTime=2026-05-01 00:00:00&endTime=2026-05-04 23:59:59

# 组合查询
GET /log/listPage?pageNum=1&pageSize=10&username=admin&startTime=2026-05-01 00:00:00&endTime=2026-05-04 23:59:59
```

**返回示例：**
```json
{
  "code": 200,
  "data": {
    "records": [
      {
        "id": 1,
        "username": "admin",
        "operation": "新增商品",
        "method": "com.wms.controller.GoodsController.save",
        "params": "[{\"id\":null,\"name\":\"测试商品\",\"barcode\":\"123456\"}]",
        "ip": "127.0.0.1",
        "createTime": "2026-05-04T10:30:00",
        "executionTime": 45
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

## 使用方法

### 在Controller中添加日志记录

只需在需要记录日志的方法上添加 `@Log` 注解即可：

```java
import com.wms.common.Log;

@RestController
@RequestMapping("/goods")
public class GoodsController {
    
    @Autowired
    private IGoodsService goodsService;
    
    // 新增商品 - 自动记录日志
    @Log("新增商品")
    @PostMapping("/save")
    public Result save(@RequestBody Goods goods){
        return goodsService.save(goods) ? Result.suc() : Result.fail();
    }
    
    // 更新商品 - 自动记录日志
    @Log("更新商品")
    @PostMapping("/update")
    public Result update(@RequestBody Goods goods){
        return goodsService.updateById(goods) ? Result.suc() : Result.fail();
    }
    
    // 删除商品 - 自动记录日志
    @Log("删除商品")
    @GetMapping("/del")
    public Result del(@RequestParam String id){
        return goodsService.removeById(id) ? Result.suc() : Result.fail();
    }
}
```

### 已添加日志的Controller

目前已在 `GoodsController` 中添加了日志记录：
- ✅ 新增商品
- ✅ 更新商品
- ✅ 删除商品

您可以按照相同的方式在其他Controller中添加日志记录，例如：
- PurchaseController（采购管理）
- SalesController（销售管理）
- UserController（用户管理）
- StorageController（仓库管理）
- 等等...

## 技术特点

1. **无侵入性**：只需添加注解，无需修改业务代码
2. **异步记录**：日志保存在线程中异步执行，不影响主业务流程性能
3. **完整信息**：记录操作人、操作描述、方法路径、请求参数、IP地址、执行耗时
4. **灵活查询**：支持按操作人、时间范围进行条件查询和分页
5. **高性能**：使用AOP切面，只在需要记录日志的方法上生效

## 注意事项

1. **Session中的用户名**：当前实现从 `request.getSession().getAttribute("username")` 获取用户名，请确保登录时将用户名存入Session。如果获取不到，会记录为 "anonymous"。

2. **异步线程**：日志保存使用新线程异步执行，如果需要在高并发场景下优化，可以考虑使用线程池。

3. **参数字段长度**：`params` 字段使用 TEXT 类型，可以存储较长的JSON数据。

4. **索引优化**：已为 `username` 和 `create_time` 字段添加索引，提升查询性能。

## 扩展建议

### 1. 使用线程池优化异步日志保存

```java
// 在LogAspect中注入线程池
@Autowired
private ThreadPoolTaskExecutor taskExecutor;

// 使用线程池执行
taskExecutor.execute(() -> {
    try {
        sysLogService.saveLog(sysLog);
    } catch (Exception e) {
        e.printStackTrace();
    }
});
```

### 2. 添加日志级别

可以在 `@Log` 注解中添加级别属性：
```java
@Log(value = "新增商品", level = LogLevel.INFO)
```

### 3. 定期清理日志

可以添加定时任务，定期清理过期日志：
```java
@Scheduled(cron = "0 0 2 * * ?") // 每天凌晨2点执行
public void cleanOldLogs() {
    // 删除90天前的日志
    LocalDateTime threshold = LocalDateTime.now().minusDays(90);
    sysLogMapper.delete(new LambdaQueryWrapper<SysLog>()
        .lt(SysLog::getCreateTime, threshold));
}
```

## 依赖说明

已在 `pom.xml` 中添加 AOP 依赖：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>
```

## 总结

操作日志功能已完整实现，包括：
- ✅ 数据库表设计（sys_log）
- ✅ 自定义注解（@Log）
- ✅ AOP切面类（LogAspect）
- ✅ 实体类（SysLog）
- ✅ Mapper接口（SysLogMapper）
- ✅ Service层（ISysLogService + SysLogServiceImpl）
- ✅ Controller查询接口（SysLogController）
- ✅ 使用示例（GoodsController）

您现在可以在任何需要记录操作日志的Controller方法上添加 `@Log` 注解，系统会自动记录详细的操作信息。