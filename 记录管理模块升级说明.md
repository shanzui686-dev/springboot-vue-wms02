# 记录管理模块升级说明

## 一、升级概述

本次升级对WMS系统的"记录管理"（出入库流水记录）模块进行了全面优化，新增了操作类型和关联单据号字段，增强了查询功能，并提供了数据导出能力。

## 二、主要变更

### 1. 数据库实体扩展

在 `Record` 实体中新增两个关键字段：

- **operation_type** (操作类型)：标识记录的操作类型，如采购入库、销售出库、退货入库、盘点盈亏等
- **ref_order_num** (关联单据流水号)：关联的业务单据编号，便于追溯

**数据库迁移脚本**：`src/main/resources/sql/add_operation_fields_to_record.sql`

```sql
ALTER TABLE record 
ADD COLUMN operation_type VARCHAR(50) DEFAULT NULL COMMENT '操作类型' AFTER status,
ADD COLUMN ref_order_num VARCHAR(100) DEFAULT NULL COMMENT '关联单据流水号' AFTER operation_type;
```

### 2. 优化分页查询接口 (/record/listPageCC)

#### 新增查询参数

支持以下高级查询条件：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| name | String | 商品名称（模糊查询） |
| goodstype | Integer | 分类ID |
| storage | Integer | 仓库ID |
| operationType | String | 操作类型（精确匹配） |
| startDate | String | 开始时间（格式：yyyy-MM-dd HH:mm:ss） |
| endDate | String | 结束时间（格式：yyyy-MM-dd HH:mm:ss） |
| roleId | String | 角色ID |
| userId | String | 用户ID |

#### 时间范围查询

使用 MyBatis Plus 的 `ge` (>=) 和 `le` (<=) 方法对 `create_time` 进行区间过滤：

```java
if(StringUtils.isNotBlank(startDate) && !"null".equals(startDate)){
    queryWrapper.ge("a.createtime", startDate);
}
if(StringUtils.isNotBlank(endDate) && !"null".equals(endDate)){
    queryWrapper.le("a.createtime", endDate);
}
```

### 3. 数据导出接口 (/record/export)

#### 接口说明

- **请求方式**：POST
- **请求路径**：/record/export
- **请求参数**：与 listPageCC 相同的查询条件
- **响应类型**：Excel文件流（.xlsx格式）

#### 功能特性

1. **使用 EasyExcel 生成 Excel 文件**
   - 依赖版本：com.alibaba:easyexcel:3.3.2
   
2. **导出字段**（共12列）：
   - 记录ID
   - 商品名称
   - 仓库名称
   - 分类名称
   - 操作类型
   - 关联单据号
   - 数量
   - 申请人
   - 操作人
   - 操作时间
   - 备注
   - 状态（中文描述）

3. **文件名格式**：`出入库记录_{时间戳}.xlsx`

4. **状态转换**：
   - 0 → 待审核
   - 1 → 已完成
   - 2 → 已拒绝

#### 使用示例

```javascript
// 前端调用示例
axios.post('/record/export', {
  pagenum: 1,
  pagesize: 10,
  param: {
    name: '商品A',
    operationType: '采购入库',
    startDate: '2026-05-01 00:00:00',
    endDate: '2026-05-05 23:59:59'
  }
}, {
  responseType: 'blob'
}).then(response => {
  const url = window.URL.createObjectURL(new Blob([response.data]));
  const link = document.createElement('a');
  link.href = url;
  link.setAttribute('download', '出入库记录.xlsx');
  document.body.appendChild(link);
  link.click();
});
```

## 三、文件清单

### 修改的文件

1. **实体类**
   - `Record.java` - 新增 operationType 和 refOrderNum 字段及getter/setter
   - `RecordRes.java` - 继承Record，自动获得新字段

2. **Mapper XML**
   - `RecordMapper.xml` - 更新 ResultMap 映射新字段

3. **控制器**
   - `RecordController.java` 
     - 优化 listPageCC 方法，支持高级查询
     - 新增 export 方法，实现Excel导出

4. **依赖配置**
   - `pom.xml` - 添加 EasyExcel 依赖

### 新增的文件

1. **实体类**
   - `RecordExportVO.java` - Excel导出专用VO，包含@ExcelProperty注解

2. **SQL脚本**
   - `add_operation_fields_to_record.sql` - 数据库表结构变更脚本

## 四、部署步骤

### 1. 执行数据库迁移

```bash
# 连接到数据库后执行
mysql -u your_username -p your_database < src/main/resources/sql/add_operation_fields_to_record.sql
```

### 2. 更新Maven依赖

```bash
cd wms
mvn clean install
```

### 3. 重启应用

```bash
# 停止应用
# 启动应用
java -jar target/wms-0.0.1-SNAPSHOT.jar
```

## 五、注意事项

1. **兼容性**：新增字段允许为NULL，不影响现有数据
2. **性能优化**：如果经常按 operation_type 或 ref_order_num 查询，建议添加索引
3. **字符编码**：导出文件名使用UTF-8编码，确保中文正常显示
4. **大数据量导出**：当前实现一次性加载所有数据到内存，如果数据量超过10万条，建议改用流式导出

## 六、后续优化建议

1. **添加索引**：
   ```sql
   CREATE INDEX idx_operation_type ON record(operation_type);
   CREATE INDEX idx_ref_order_num ON record(ref_order_num);
   CREATE INDEX idx_createtime ON record(createtime);
   ```

2. **异步导出**：对于大数据量场景，建议实现异步导出+消息通知机制

3. **操作类型枚举**：创建 OperationTypeEnum 枚举类，规范操作类型的取值

4. **导出模板定制**：支持用户自定义导出列

## 七、测试建议

1. **单元测试**：测试各个查询条件的组合
2. **集成测试**：验证导出文件的正确性和完整性
3. **性能测试**：测试大量数据导出的响应时间和内存占用
4. **边界测试**：测试空结果集、特殊字符等情况

## 八、技术栈

- Spring Boot 3.2.0
- MyBatis Plus 3.5.7
- EasyExcel 3.3.2
- Jakarta Servlet (Spring Boot 3.x)
- MySQL
