# 操作日志查看页面使用说明

## 功能概述

基于 Vue 3 + Element Plus 开发的操作日志查看页面，提供完整的日志查询、展示和详情查看功能。

## 文件位置

```
workspace/wms-web/src/components/system/OperationLog.vue
```

## 主要功能

### 1. 顶部搜索区

包含以下搜索条件：

- **操作人输入框**：支持按操作人姓名模糊查询
- **操作时间选择器**：使用 `el-date-picker` 的 `daterange` 类型，可选择开始日期和结束日期
- **查询按钮**：执行搜索操作
- **重置按钮**：清空所有搜索条件并重新加载数据

### 2. 主表格展示

展示以下字段：

| 字段 | 说明 | 特性 |
|------|------|------|
| ID | 日志ID | 居中对齐 |
| 操作人 | 执行操作的用户名 | 居中对齐 |
| 操作描述 | 操作的具体描述 | 超长文本显示省略号+tooltip |
| 请求方法 | 完整的方法路径 | 超长文本显示省略号+tooltip，等宽字体蓝色显示 |
| 请求参数 | 参数预览 | 显示"查看详情"按钮，点击弹出对话框 |
| 操作IP | 客户端IP地址 | 居中对齐 |
| 执行耗时 | 方法执行时间 | 根据耗时长短显示不同颜色标签：<br/>- 绿色：< 500ms<br/>- 黄色：500-1000ms<br/>- 红色：> 1000ms |
| 操作时间 | 日志创建时间 | 格式化为 `yyyy-MM-dd HH:mm:ss` |

### 3. 详情对话框

点击"查看详情"按钮后弹出对话框，展示：

- **基本信息**：使用 `el-descriptions` 组件展示操作人、操作描述、请求方法、IP、耗时、时间
- **请求参数**：以格式化的 JSON 形式展示，支持滚动查看长参数

### 4. 底部分页

标准的 `el-pagination` 组件，支持：

- 显示总记录数
- 调整每页显示条数（10, 20, 50, 100）
- 上一页/下一页
- 页码跳转
- 快速跳转到指定页

## 使用方法

### 1. 在路由中配置

在 `workspace/wms-web/src/router/index.js` 中添加路由配置：

```javascript
{
  path: '/operation-log',
  name: 'OperationLog',
  component: () => import('@/components/system/OperationLog.vue'),
  meta: {
    title: '操作日志'
  }
}
```

### 2. 在菜单中添加

根据您的菜单系统，在侧边栏或导航菜单中添加"操作日志"菜单项，链接到 `/operation-log` 路由。

### 3. 直接访问

启动前端项目后，访问：
```
http://localhost:8080/#/operation-log
```
（具体端口和路径根据您的配置可能有所不同）

## 代码特点

### 1. Composition API

使用 Vue 3 的 `<script setup>` 语法，代码更简洁：

```vue
<script setup>
import { ref, onMounted, getCurrentInstance } from 'vue'
// ...
</script>
```

### 2. 响应式数据

使用 `ref` 创建响应式数据：

```javascript
const tableData = ref([])
const loading = ref(false)
const total = ref(0)
```

### 3. 日期格式化

提供两个日期格式化函数：

- `formatDate()`：格式化日期为 `yyyy-MM-dd`
- `formatDateTime()`：格式化日期时间为 `yyyy-MM-dd HH:mm:ss`

### 4. JSON 格式化

`formatJson()` 函数将 JSON 字符串格式化为美观的多行格式，便于阅读：

```javascript
const formatJson = (jsonStr) => {
  if (!jsonStr) return '-'
  try {
    const obj = JSON.parse(jsonStr)
    return JSON.stringify(obj, null, 2)
  } catch (e) {
    return jsonStr
  }
}
```

### 5. 执行耗时可视化

根据执行耗时显示不同颜色的标签：

```vue
<el-tag :type="scope.row.executionTime > 1000 ? 'danger' : scope.row.executionTime > 500 ? 'warning' : 'success'">
  {{ scope.row.executionTime }} ms
</el-tag>
```

### 6. 加载状态

表格使用 `v-loading` 指令显示加载状态，提升用户体验。

### 7. 响应式设计

通过 CSS 媒体查询实现移动端适配：

```css
@media (max-width: 768px) {
  .search-form {
    flex-direction: column;
  }
}
```

## API 调用

页面调用后端接口：

```
GET /log/listPage
```

**请求参数：**
- `pageNum`: 页码
- `pageSize`: 每页大小
- `username`: 操作人（可选）
- `startTime`: 开始时间（可选，格式：yyyy-MM-dd HH:mm:ss）
- `endTime`: 结束时间（可选，格式：yyyy-MM-dd HH:mm:ss）

**返回示例：**
```json
{
  "code": 200,
  "data": {
    "records": [
      {
        "id": 1,
        "username": "admin",
        "operation": "新增商品",
        "method": "com.wms.controller.GoodsController.save",
        "params": "[{\"name\":\"测试商品\"}]",
        "ip": "127.0.0.1",
        "createTime": "2026-05-04T10:30:00",
        "executionTime": 45
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

## 样式说明

### 1. 卡片布局

使用 `el-card` 组件将搜索区和表格区分开，增加视觉层次。

### 2. 表格样式

- 表头背景色：`#f5f7fa`
- 表头文字颜色：`#606266`
- 边框：有
- 斑马纹：有

### 3. 请求方法样式

使用等宽字体和蓝色显示，突出技术感：

```css
.method-text {
  font-family: 'Courier New', monospace;
  font-size: 12px;
  color: #409eff;
}
```

### 4. JSON 内容样式

格式化后的 JSON 参数显示区域：

- 背景色：`#f5f7fa`
- 等宽字体
- 最大高度 400px，超出可滚动
- 自动换行

## 注意事项

1. **只读功能**：此页面仅提供查看功能，不包含新增、修改、删除操作。

2. **权限控制**：如需限制访问权限，请在路由守卫或菜单系统中配置。

3. **大数据量优化**：如果日志数据量很大，建议：
   - 限制单次查询的时间范围
   - 添加更多的筛选条件
   - 考虑后端分页优化

4. **日期选择器**：时间范围查询时，会自动将开始时间设置为 `00:00:00`，结束时间设置为 `23:59:59`。

5. **空值处理**：所有字段都做了空值判断，避免显示 `null` 或 `undefined`。

## 扩展建议

### 1. 导出功能

可以添加导出 Excel 功能：

```javascript
const exportLogs = () => {
  // 调用后端导出接口
  axios.get(httpUrl + '/log/export', {
    params: { /* 搜索条件 */ },
    responseType: 'blob'
  }).then(res => {
    // 下载文件
  })
}
```

### 2. 批量删除

如果需要清理日志，可以添加批量删除功能（需要后端支持）。

### 3. 高级筛选

可以添加更多筛选条件：
- 操作类型（增删改查）
- 执行耗时范围
- 请求方法关键字

### 4. 实时刷新

可以添加定时刷新功能：

```javascript
import { onUnmounted } from 'vue'

let timer = null

onMounted(() => {
  loadLogs()
  // 每30秒刷新一次
  timer = setInterval(loadLogs, 30000)
})

onUnmounted(() => {
  if (timer) {
    clearInterval(timer)
  }
})
```

## 总结

OperationLog.vue 页面已完整实现，包含：

- ✅ 顶部搜索区（操作人、时间范围、查询/重置按钮）
- ✅ 主表格展示（8个字段，带tooltip和查看详情按钮）
- ✅ 详情对话框（格式化JSON参数）
- ✅ 底部分页（标准el-pagination）
- ✅ 响应式设计
- ✅ 加载状态
- ✅ 空值处理
- ✅ 执行耗时可视化

页面代码简洁、功能完整、用户体验良好，可直接集成到您的项目中。
